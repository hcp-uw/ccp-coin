/**
 * Signature gold gradient line motif used as a visual divider between sections.
 * 1px height, full-width horizontal gradient from transparent through gold to transparent.
 */
export function GoldThread() {
  return (
    <div
      className="gold-thread"
      role="separator"
      aria-hidden="true"
    />
  );
}
